# notifyMe_Server
This is the server side code of the NotifyMe app ready to be deployed on Google® Cloud.
To use this code and deploy your own server obtain a json key from the gcloud console and place it into the project (setting the relative env. variable)

This server is meant to be accessed from the PC client to get all users information and from the Android® client to register itself
